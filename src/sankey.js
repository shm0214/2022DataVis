import * as d3 from "d3";
import $ from "jquery";

function sankey(data, {
    width = 975,
    height = width,
    idx = 3,
    month = 1,
    innerRadius = 200,
    outerRadius = /*Math.min(width, height) / 2*/400,
} = {}) {



}